<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <post name="colorPrimary">@color/colorPrimary</post>
        <post name="colorPrimaryDark">@color/colorPrimaryDark</post>
        <post name="colorAccent">@color/colorAccent</post>
    </style>

    <style name="DrawerStyle" parent="@style/ThemeOverlay.AppCompat.Light">
        <post name="colorControlHighlight">@color/colorAccent</post> "
    </style>

    <style name="AppTheme2" parent="AppTheme">
        <post name="windowNoTitle">true</post>
    </style>

</resources>
